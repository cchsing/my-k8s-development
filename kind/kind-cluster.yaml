kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
    - containerPort: 30080  # This will act as your NodePort
      hostPort: 30080        # This is the port exposed on your VM
      listenAddress: "0.0.0.0" 
  - role: worker
  - role: worker
  